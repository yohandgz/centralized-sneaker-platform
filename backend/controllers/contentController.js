const { getYoutubeData } = require('../utils/apiClient.js')
const db = require('../config/db')

const getContent = async (req, res) => {
  // implemennt logic here.
}

module.exports = {
  getContent
}